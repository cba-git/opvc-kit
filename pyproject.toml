[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "opvc"
version = "0.1.0"
description = "OPVC collaboration skeleton"
requires-python = ">=3.9"
